name: Laravel Pint

on:
  pull_request:
    branches: [main, develop]

jobs:
  pint:
    runs-on: ubuntu-latest
    steps:
      - Setup PHP
      - Install dependencies
      - Run: ./vendor/bin/pint --test
      - Comment on PR if style issues found
